import Grid from './grid.js';

export default class Maze extends Grid {
    constructor(width, height, start, end) {
        super(width, height);
        this._start = start;
        this._end = end;
    }
    // get start
    // pointStartFinish(startVector, finishVector) {
    //     this.setValue
    // }

    // clear

}